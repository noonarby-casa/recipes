baseURL = 'https://recipes.noonarby.casa/'
locale = 'en-us'
title = 'Recipes'
theme = 'cookpot'

[pagination]
  pagerSize = 10

[outputs]
  home = ["HTML", "RSS", "JSON"]

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
